import { CurrencyBySitePipe } from './currency-by-site.pipe';

describe('CurrencyBySitePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyBySitePipe();
    expect(pipe).toBeTruthy();
  });
});
